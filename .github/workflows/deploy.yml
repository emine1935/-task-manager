# OPSÄ°YONEL: CI/CD ile otomatik deploy istiyorsanÄ±z kullanÄ±n.
# Ä°stemiyorsanÄ±z bu dosyayÄ± silip sadece manuel "ssh + ./deploy.sh" ile de
# ilerleyebilirsiniz â€” deploy.sh script'i her iki durumda da aynÄ±dÄ±r.
#
# Gerekli GitHub Secrets (Repo > Settings > Secrets and variables > Actions):
#   SERVER_HOST       -> Sunucunun IP adresi
#   SERVER_USER       -> SSH kullanÄ±cÄ± adÄ±
#   SERVER_SSH_KEY    -> SSH private key (id_ed25519 iÃ§eriÄŸi)
#   SERVER_APP_DIR    -> Sunucuda projenin bulunduÄŸu klasÃ¶r (Ã¶rn. /opt/task-manager)

name: Deploy to Server

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH ile sunucuya baÄŸlan ve deploy et
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd ${{ secrets.SERVER_APP_DIR }}
            ./deploy.sh main


